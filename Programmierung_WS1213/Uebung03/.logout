# @(#) $Header: 4.0 Standard ~/.logout$
# November 1995 Dipl. Phys. Thomas Scheunemann    HRZ Uni -GH- Duisburg
# Diese Datei sollte vom Benutzer nicht geaendert werden, um ein spaeteres
# Ersetzen durch eine neuere Version zu Ermoeglichen. Wenn Sie Anpassungen
# vornehmen wollen, legen Sie eine Datei .mylogout an. Diese wird, wenn sie
# vorhanden ist im Anschluss an dieses Skript ausgefuehrt.

# Zur Referenz
if( ! $?RC_FILES ) then
	setenv RC_FILES .logout
else
	setenv RC_FILES ${RC_FILES}:.logout
endif

# Default HOME
if ( ! $?HOME) then
	setenv HOME /
endif

# Fuehre nun die Benutzereigene Datei .mylogout aus
if ( -r $HOME/.mylogout ) then
	source $HOME/.mylogout
endif
